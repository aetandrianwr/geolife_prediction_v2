# Model v2: 88d, 4 layers (BEST MODEL)
# Test Acc@1: 37.95%
# Test Acc@5: 56.54%
# Test MRR: 46.39%
# Val Acc@1: 43.70%
# Parameters: 481,458

model:
  name: model_v2
  d_model: 88
  d_inner: 176
  n_layers: 4
  n_head: 8
  d_k: 11
  d_v: 11
  dropout: 0.15
  max_len: 50

training:
  batch_size: 64
  learning_rate: 0.0005
  weight_decay: 0.0001
  label_smoothing: 0.1
  max_epochs: 200
  patience: 30
  log_interval: 50

data:
  data_dir: /content/another_try_20251125/data/geolife
  max_len: 50
  num_workers: 0

experiment:
  name: model_v2
  seed: 42
  device: cuda
  checkpoint_dir: checkpoints
  log_dir: logs
  result_dir: results
  save_best_only: true
  deterministic: true
